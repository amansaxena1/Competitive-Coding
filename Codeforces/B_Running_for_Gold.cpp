#include <bits/stdc++.h>
#define llint long long int
using namespace std;
void aman()
{
    llint n;
    cin >> n;
    llint w[5];
    llint a[n][5];
    for (llint i = 0; i < n; i++)
    {
        for (llint j = 0; j < 5; j++)
        {
            cin >> a[i][j];
        }
    }
    llint mini = INT_MIN;
    for (llint i = 0; i < 5; i++)
    {
        for (llint j = 0; j < n; j++)
        {
            if (a[][i])
            {
            }
        }
    }
}
int main()
{
    llint t, n, a;
    cin >> t;
    while (t--)
    {
        aman();
    }
    return 0;
}