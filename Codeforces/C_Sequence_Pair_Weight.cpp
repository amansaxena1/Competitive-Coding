#include <bits/stdc++.h>
#define llint long long int
using namespace std;

int main()
{
    llint t, n, a;
    cin >> t;
    while (t--)
    {
        vector<llint> v;
        cin >> n;
        for (llint i = 0; i < v.size(); i++)
        {
            cin >> a;
            v.push_back(a);
        }
    }
}